version: "3.9"

services:

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app   # 🔥 keep for live changes
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"